import connect from "react-redux/es/connect/connect";
import TitleQuestionTab from "../../component/tabs/TitleQuestion";

const TitleQuestionImpl = connect(
    null,
    {}
)(TitleQuestionTab);
export default TitleQuestionImpl;
