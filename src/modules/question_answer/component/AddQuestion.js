import React from 'react';
import BasicComponent from "../../../common/abstract/component/BasicComponent";

export default class AddQuestion extends BasicComponent {
    render() {
        return (
            <section>Add Question UI Here</section>
        )
    }
}